git checkout -b branch_name # creates new branch and goes into it
git branch # tells you which branch youre in


git status # shows chages
git pull # pulls the changes for that branch that are not up to date


git add .
git commit -m "msg"
git push origin branch_name # pushes the changes inside that branch

